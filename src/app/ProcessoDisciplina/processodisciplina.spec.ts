import { ProcessoDisciplina } from "../models/processo-disciplina";


describe('Processodisciplina', () => {
  it('should create an instance', () => {
    expect(new ProcessoDisciplina()).toBeTruthy();
  });
});
