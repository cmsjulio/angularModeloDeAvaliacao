import { ProcessoAvaliativo } from '../models/processo-avaliativo';

describe('ProcessoAvaliativo', () => {
  it('should create an instance', () => {
    expect(new ProcessoAvaliativo()).toBeTruthy();
  });
});
